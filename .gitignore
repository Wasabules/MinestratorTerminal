# --- Dépendances ---
node_modules

# --- Build front (SvelteKit / Vite) — desktop/ et mobile/ ---
build/
.svelte-kit/
/package
dist
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# --- Build Rust / Tauri ---
/target
# Cibles et schémas Tauri générés, quelle que soit l'app (desktop/ ou mobile/)
**/src-tauri/target
**/src-tauri/gen/schemas

# --- Environnement / secrets ---
.env
.env.*
!.env.example

# --- Claude Code (sessions, mémoire, historique local) ---
.claude

# --- Références tierces / brouillons (hors dépôt public) ---
MCP_MINESTRATOR.txt
message(1).txt

# --- Clés de signature de l'updater (JAMAIS dans le dépôt) ---
*.key
*.pem

# --- OS / éditeurs / divers ---
.DS_Store
Thumbs.db
*.log
